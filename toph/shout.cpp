#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    string input;
    getline(cin, input); // সম্পূর্ণ লাইন ইনপুট নেওয়া
    cout << input << endl; // ইনপুট প্রিন্ট করা
    return 0;
}